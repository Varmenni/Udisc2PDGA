{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run convert.py",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/convert.py",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}
